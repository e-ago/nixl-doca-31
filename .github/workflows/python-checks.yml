name: Python Checks

on: [pull_request]

jobs:
  python-checks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 2
    - name: Check for print() calls
      run: |
        ./.gitlab/check_prints.sh ./src
        ./.gitlab/check_prints.sh ./test
